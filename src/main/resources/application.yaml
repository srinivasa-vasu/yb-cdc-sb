server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todo
    username: root
    password: root
  jpa.hibernate.ddl-auto: create-drop
  jpa.show-sql: true

## Source Database Properties
supplier:
  datasource:
    host: 127.0.0.2
    port: 5433
    database: yugabyte
    username: yugabyte
    password: yugabyte
    streamId: 54470c729c694eebae7facf9e0241721
    tableList: public.todo

solace:
  jms:
    host: smf://localhost:55554
    msgVpn: default
    clientUsername: default
    clientPassword: default

target:
  dest: solace-test
